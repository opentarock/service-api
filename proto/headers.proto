package proto_headers;

message AuthorizationHeader {
  required uint64 user_id = 1;
  required string access_token = 2;
}

message RequestCorrelationHeader {
  required string correlation_id = 1;
}

message TimeoutHeader {
  optional uint64 timeout = 1;
  optional uint64 deadline = 2;
}
